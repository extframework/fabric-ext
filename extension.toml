[repositories]

[parents]
":mappings" = {isBuild = true}
minecraft = { group = "dev.extframework.core", version = "1.0.6-BETA", use = "central" }
access-tweaks = { group = "dev.extframework.extension", version = "1.0.6-BETA", use = "central" }
