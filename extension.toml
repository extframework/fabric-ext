[repositories]
local = true

[parents]
":mappings" = {isBuild = true}
minecraft = { group = "com.kaolinmc.core", version = "1.0.7-BETA", use = "central" }
access-tweaks = { group = "com.kaolinmc.extension", version = "1.0.6-BETA", use = "central" }
